<!DOCTYPE html>
<html>
#set(components = ["bootstrap", "layout", "tag"])
#set(GroupPreview group)
#set(Collection<GroupTopicPreview> topics)
<head>
  <meta charset="UTF-8">
  <title>{${group.name}}小组 - 紫月空</title>
  $!{includeCSS()}
  <style>
    .topic{
      background: #f0f0f0
    }
  </style>
</head>
<body>
$!{include("navbar.httl")}
  <div id="container" class="container">
    <div class="group">
      <p>${group.name}</p>
      介绍: <p>${group.introduction}</p>
      #for(each : group.tags)
       $!{include("tag-label.httl")}
      #end
    </div>
    <a class="btn btn-primary btn-sm" href="/write-blog?groupId=${group.id}">发表文章!</a>
    #for(topic : topics)
      <div class="topic">˜
        <a href="/topic/${topic.id}">${topic.blog.title}</a>
        <p>${topic.blog.preview}</p>
        <a>${topic.blog.author.name}</a>
        <span>${topic.blog.createdTime.format("yyyy-MM-dd HH:mm:ss")}</span>
      </div>
    #end
  </div>
$!{includeProto}
$!{includeJS()}
</body>
</html>